
import { calculateChange } from 'frontend/helpers/calculate-change';
import { module, test } from 'qunit';

module('Unit | Helper | calculate change');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = calculateChange([42]);
  assert.ok(result);
});

