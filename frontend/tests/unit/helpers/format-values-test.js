
import { formatValues } from 'frontend/helpers/format-values';
import { module, test } from 'qunit';

module('Unit | Helper | format values');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatValues([42]);
  assert.ok(result);
});

