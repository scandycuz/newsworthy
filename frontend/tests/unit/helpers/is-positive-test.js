
import { isPositive } from 'frontend/helpers/is-positive';
import { module, test } from 'qunit';

module('Unit | Helper | is positive');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isPositive([42]);
  assert.ok(result);
});

